import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Planes de Precios',
    description: 'Explora nuestros planes de precios y elige el que se adapte a tus necesidades.',
    keywords: ["precios", "planes", "suscripciones", "costos", "tabla de precios", "..."]
  };

export default function PricingPage() {
    return (
      <span className="text-5xl">
      Pricing Page
      </span>
    );
}
